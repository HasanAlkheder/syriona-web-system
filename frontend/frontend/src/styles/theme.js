// src/styles/theme.js

const theme = {
  colors: {
    primary: "#0A2540",       // Dark blue (brand)
    primaryLight: "#13335A",  // Slightly lighter shade
    white: "#FFFFFF",
    textDark: "#0A2540",
    textLight: "#F0F4F8",
    sidebarText: "#E2E8F0",
    background: "#F4F7FA",
    border: "#D1D5DB",
  },
  spacing: (value) => `${value * 8}px`,
};

export default theme;
